{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://learning-orbit.local/schemas/student-visible-promotion-record.v1.json",
  "title": "StudentVisiblePromotionRecord",
  "description": "The separate decision that lets students see the analytics built from their own conversation. It is never inferred from a completed shadow: \"the agent behaved acceptably in a room with one adult in it\" and \"students may see their own analytics\" are different questions. Default-deny is the resting state, so the record must name the projection keys out loud - an empty list is chat-only, and no list at all is refused.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "recordKind",
    "promotionId",
    "roomId",
    "synthetic",
    "externalAuthorizationRecordSha256",
    "authorizedFrom",
    "authorizedUntil",
    "shadowRecordSha256",
    "shadowTeacherRef",
    "shadowVerdict",
    "derivedFromShadowRecord",
    "decidedBy",
    "studentProjectionKeys",
    "startsAt",
    "expiresAt",
    "revocation",
    "usedForGradesOrDiscipline"
  ],
  "properties": {
    "recordKind": { "const": "student_visible_promotion" },
    "promotionId": { "type": "string", "format": "uuid" },
    "roomId": {
      "description": "The one room this decision covers. Promotion is a whole-room decision and is never made for a cohort in the abstract.",
      "type": "string",
      "format": "uuid"
    },
    "synthetic": {
      "description": "A synthetic record is a rehearsal of the paperwork and is not a promotion. True is refused.",
      "type": "boolean"
    },
    "externalAuthorizationRecordSha256": {
      "description": "The digest of the signed external authorization this promotion rests on.",
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "authorizedFrom": {
      "description": "The start of the window that authorization granted, restated so this record can be refused on its own. The importer must still compare it with the verified authorization record; a restatement is not the record.",
      "type": "string",
      "format": "date-time"
    },
    "authorizedUntil": { "type": "string", "format": "date-time" },
    "shadowRecordSha256": {
      "description": "The digest of the signed completed shadow. Named so the two decisions can be linked, not so one can be derived from the other.",
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "shadowTeacherRef": {
      "description": "The teacher who ran that shadow, as a salted digest. A promotion signed by that same person is refused: one person answering both questions at once tends to answer the second by momentum.",
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "shadowVerdict": { "enum": ["ready_for_students", "not_ready"] },
    "derivedFromShadowRecord": {
      "description": "Whether this promotion was derived from the shadow rather than decided. True is refused.",
      "type": "boolean"
    },
    "decidedBy": {
      "description": "Who decided, as a salted digest, and when.",
      "type": "object",
      "additionalProperties": false,
      "required": ["deciderRef", "deciderRole", "decidedAt"],
      "properties": {
        "deciderRef": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
        "deciderRole": { "enum": ["school_authority", "research_ethics_board", "designated_release_custodian"] },
        "decidedAt": { "type": "string", "format": "date-time" }
      }
    },
    "studentProjectionKeys": {
      "description": "The projections a student in this room may be shown. Zero, one or both, named explicitly: an empty list is a promotion to chat-only operation, which is the resting state anyway.",
      "type": "array",
      "maxItems": 2,
      "uniqueItems": true,
      "items": { "enum": ["echo.student_approved", "trace.student_bundle"] }
    },
    "startsAt": { "type": "string", "format": "date-time" },
    "expiresAt": { "type": "string", "format": "date-time" },
    "revocation": {
      "description": "How this visibility is withdrawn before it expires. A path that cannot act inside the window it governs is not a revocation path.",
      "type": "object",
      "additionalProperties": false,
      "required": ["contactRef", "method", "maxLatencyMinutes"],
      "properties": {
        "contactRef": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
        "method": { "enum": ["signed_revocation_record", "operator_revoke_command"] },
        "maxLatencyMinutes": { "type": "integer", "minimum": 1, "maximum": 1440 }
      }
    },
    "usedForGradesOrDiscipline": {
      "description": "Whether what the student is shown feeds grades or discipline. True is refused.",
      "type": "boolean"
    }
  }
}
