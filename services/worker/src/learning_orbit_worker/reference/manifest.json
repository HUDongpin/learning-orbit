{
  "referenceVersion": "1.1",
  "sourcePath": "work/learning_orbit_algorithms.py",
  "sourceTestPath": "work/test_learning_orbit_algorithms.py",
  "destinationPath": "services/worker/src/learning_orbit_worker/reference/learning_orbit_algorithms_v1.py",
  "sha256": "e88e910bb40db1c304cd3d4a0154bcd0e6c0cb85dd97c5c41adfdab0f23715e1",
  "sourceTestSha256": "59ad56baa784fa187b6ea6a7cffcbba6138bfc945e43a2aa38fc6cce78560732",
  "upstreamSha256": "3a2983b0f99cd016b45fb5fd7ee8e1ac4b93b3eee62f3a189a8e20df8c1cf220",
  "divergesFromUpstreamSource": true,
  "patches": [
    {
      "id": "cm-merge-rekey",
      "target": "StreamingConceptMap.merge_concepts / undo_merge",
      "defect": "Both cleared the whole _visible_edge_ids set, dropping the theta_off hysteresis band for every edge in the room. Unrelated edges being held above theta_off disappeared at the next snapshot, and because the post-loop archive diff was then empty they were never recorded in archivedEdgeIds.",
      "fix": "Snapshot each ACTIVE contribution's edge id before the alias mutation and carry _visible_edge_ids / archived_edge_ids through the resulting map. The inverse is one-to-many, so an old edge id carries its state to every successor rather than to whichever contribution was iterated last; inactive contributions are excluded so a retracted one cannot redirect a live edge's hysteresis. Edges untouched by the alias map to themselves and keep their band."
    },
    {
      "id": "cm-retract-tombstone",
      "target": "StreamingConceptMap.apply / _reset_runtime",
      "defect": "_deactivate only scanned contributions that already existed, so a retraction delivered before the event it targets removed nothing and the target was applied normally on arrival and stayed visible forever.",
      "fix": "Record the retracted event id in _retracted_event_ids and honour that tombstone when the target arrives, returning reason=retracted_before_arrival. The event still joins the ledger and advances the watermark; it contributes no evidence."
    },
    {
      "id": "extractor-verb-boundaries",
      "target": "DeterministicEcosystemExtractor, all six patterns",
      "defect": "No pattern put word boundaries around its verb slot, so any word merely CONTAINING a verb satisfied it: 'consume' in 'consumers' (pattern 3), 'gives' in 'forgives' (1), 'feed' in 'feedback' and 'support' in 'insupportable' (2), 'return' in 'nonreturnable' (4), 'supply' in 'oversupply' (5), 'released' in 'unreleased' (6), and 'eat' in 'heat'/'meat'/'great'/'defeat' (3). Each emitted a confident edge the sentence never asserted, with an evidence span containing no such verb.",
      "fix": "Bound every verb alternation on both sides with \\\\b, spelling out exactly the inflections the unbounded form already accepted (feeds/feeding, supported/supporting, eaten/eating, consumed, returned/returning, recycled, provided, supplied) so recall is unchanged. Verified over an exhaustive 222-sentence verb x frame corpus against the v1.0 extractor: 0 matches lost, 0 gained, 9 word-internal false positives removed. The Traditional Chinese branches are untouched."
    }
  ],
  "claimCeiling": "Original engineering synthesis and executable reference; not peer reviewed, not SOTA, not a learning-outcome or production-performance claim.",
  "mutationPolicy": "Do not edit this file for behaviour changes beyond the recorded patches. Add wrappers and adapters outside reference/; a changed hash requires a new reference version and a new analysis epoch. Every divergence from sourcePath must be listed in patches with its defect and fix."
}
